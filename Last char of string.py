# to get the last letter of the string
fruit = "banana"
length = len(fruit)
last = fruit[length-1]
print("Last character: ",last)
print(fruit[-1])   # yields last character
print(fruit[-2])   # yields second last character
